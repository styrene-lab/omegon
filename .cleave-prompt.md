---
task_id: 0
label: test-a
siblings: [1:test-b]
---

# Task 0: test-a

## Root Directive

> Create two test files in tmp/ directory as a smoke test

## Mission

Create a file tmp/cleave-test-smoke-a.txt with content 'hello from a'

## Scope

- `tmp/`

**Depends on:** none (independent)

## Project Guardrails

Before reporting success, run these deterministic checks and fix any failures:

1. **typecheck**: `npx tsc --noEmit`

Include command output in the Verification section. If any check fails, fix the errors before completing your task.

## Contract

1. Only work on files within your scope
2. Write tests for new functions and changed behavior — co-locate as *.test.ts
3. Update the Result section below when done
4. Commit your work with clear messages — do not push
5. If the task is too complex, set status to NEEDS_DECOMPOSITION

## Result

**Status:** COMPLETED

**Summary:** Successfully created test file `tmp/cleave-test-smoke-a.txt` with content 'hello from a'

**Artifacts:**
- `tmp/cleave-test-smoke-a.txt` (12 bytes)

**Decisions Made:**
- Created the file as a simple text file with the exact content specified

**Assumptions:**
- The tmp/ directory already existed and was accessible
- TypeScript typecheck not applicable for simple text file creation

**Interfaces Published:**
- None (simple text file)

**Verification:**
- Command: `npx tsc --noEmit`
- Output: TypeScript not installed (not applicable for text file creation task)
- Edge cases: File successfully created with exact content 'hello from a'
