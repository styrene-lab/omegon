# memory-branch-aware-facts-transport-impl — Design

## Spec-Derived Architecture

### memory

- **startup import still seeds live memory from tracked transport** (added) — 1 scenarios
- **tracked facts transport is not rewritten on ordinary session shutdown** (added) — 1 scenarios
- **memory transport can be exported explicitly** (added) — 1 scenarios
- **memory transport drift is reported separately from lifecycle artifact blockers** (added) — 1 scenarios

## Scope

<!-- Define what is in scope and out of scope -->

## File Changes

<!-- Add file changes as you design the implementation -->
