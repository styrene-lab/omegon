## 1. 6A — Identity, decision, and confirmation contract freeze
<!-- specs: memory/reconciliation -->

- [ ] Freeze stable candidate/evidence identities and storage-key compatibility for immutable formations and separate lifecycle pending facts.
- [ ] Freeze decision-record owner, persisted payload, operation binding, admission unit, and recovery transitions within existing persistence owners.
- [ ] Resolve refinement representation and broader-claim behavior with examples and expected history.
- [ ] Freeze confirmation binding to reviewed plan and target versions while preserving explicit-write and inferred-lifecycle policies.
- [ ] Assign nonoverlapping file owners and publish frozen interfaces before parallel implementation.

## 2. 6B — Bounded matching and decision validation
<!-- specs: memory/reconciliation -->

- [ ] Add deterministic fixtures for paraphrases, correlated versus independent sources, unknown scope, cross-platform scope, and retired-history lookup.
- [ ] Implement bounded exact/lexical/optional semantic matching and host-injected classification with typed pending failures.
- [ ] Implement validated new/equivalent/refinement/correction/conflict outcomes and authority checks against frozen 6A contracts.

## 3. 6C — Atomic admission and replay
<!-- specs: memory/reconciliation -->

- [ ] Extend existing mutation receipts and fact preconditions for atomic admission on SQLite and in-memory backends.
- [ ] Select the next available migration at implementation time; add compatibility, reopen, and transport fixtures.
- [ ] Verify changed-payload operation conflicts, same-evidence different-operation deduplication, version conflicts, and full admission-unit rollback.

## 4. 6D — Recoverable decision work
<!-- specs: memory/reconciliation -->

- [ ] Persist decision/admission progress separately from completed extraction through the managed service.
- [ ] Verify crash after decision persistence, lost acknowledgement replay, cancellation, and partial progress between admission units.
- [ ] Verify retries resume only unfinished units without re-extracting completed batches or fabricating local receipts from transported coverage.

## 5. 6E — Host, review, and read-side integration
<!-- specs: memory/reconciliation -->

- [ ] Route explicit writes, lifecycle inference, and formation candidates through shared validation with preserved admission intent.
- [ ] Invalidate approval when reviewed plans or target versions change; exercise interactive and non-interactive pending behavior.
- [ ] Expose conflict/history/pending distinctions and invalidate semantic caches after admission or relationship changes.
- [ ] Verify stale index completion rejection, current-version retrieval, and embedding-unavailable lexical behavior.

## 6. 6F — Reconciliation evaluation and landing
<!-- specs: memory/reconciliation -->

- [ ] Build separate development and held-out false-merge, duplicate, correction, authority, and downstream-task regression corpora with scenario mappings.
- [ ] Measure development baselines and freeze metric definitions, numeric thresholds, corpus digests, policy, prompts, models, and settings before held-out evaluation.
- [ ] Obtain fresh live routing and aggregate token/time authorization; report measured usage and unavailable outcomes without carrying over Wave 5's budget.
- [ ] Run deterministic backend/recovery/confirmation gates and the new held-out quality evaluation; retain raw outputs and failure attribution.
- [ ] Run applicable multi-crate landing gates and memory feature configurations, serializing main-crate tests; review composition budgets if tools or schemas change.
- [ ] Validate this change and record implementation evidence separately from OpenSpec structural validation.
