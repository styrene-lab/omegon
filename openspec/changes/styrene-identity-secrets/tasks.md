# Styrene Identity as operator credential root — RNS identity for secret unlocking and trust — Tasks

## 1. Separate secrets.db, encrypted at rest, never in git

- [ ] 1.1 Implement Separate secrets.db, encrypted at rest, never in git

## 2. Mesh secrets are live lookups, no caching

- [ ] 2.1 Implement Mesh secrets are live lookups, no caching

## 3. Three encryption backends: Styrene Identity (feature-gated), OS keyring (default), passphrase with Argon2id (default)

- [ ] 3.1 Implement Three encryption backends: Styrene Identity (feature-gated), OS keyring (default), passphrase with Argon2id (default)
