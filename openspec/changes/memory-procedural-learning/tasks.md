## 1. Freeze interfaces and dependency acceptance before parallel integration
<!-- specs: memory/procedural -->

- [ ] Confirm the `7b2da402` assessment against the implementation start revision; read root, memory, main-crate, and traits directives plus actual skills contracts.
- [ ] Record accepted Wave 6 reconciliation as the gate for automatic admission/correction and freeze the narrow Wave 7 evidence/usage, verification/validity/retention, unknown-source, and versioned-feedback contract.
- [ ] Freeze procedure/version/application identity, canonical evidence deduplication, lifecycle, retirement, feedback, migration, and backend-parity contracts.
- [ ] Freeze explicit operator approval binding and shared plan/apply/inspect/result DTOs, including opID, input/artifact hashes, procedure/evidence preconditions, target scope identity, expected absence/content hash, and replay receipts.
- [ ] Select the registered promotion command spelling and TUI/CLI remote/ACP approval transport; define unavailable behavior and routing fixtures before parallel host adapters.
- [ ] Freeze guarded publication ownership, journal ordering/durability, conflict and forward-repair semantics with the filesystem owner; exclude replacement and user/global/executable modes from this slice.

## 2. Procedure and application lifecycle
<!-- specs: memory/procedural -->

- [ ] RED: Add failed-attempt/verified-repair, scoped hypothesis, retirement/history, missing/truncated source, duplicate import, partial execution, unused selection, and tool-success/failed-postcondition fixtures.
- [ ] GREEN: Extend `types.rs`, `backend.rs`, SQLite migrations, and `InMemoryBackend` with immutable procedure versions and separate application/feedback identities; preserve old deserialization and reopen behavior.
- [ ] GREEN: Bind feedback to canonical session/stream/event sequences, selected version, environment, and verification run; preserve unknown sources and idempotent imports without support inflation.
- [ ] GREEN: Route automatic admission/correction through accepted reconciliation; preserve counterexample and supersession history under the frozen retention contract.
- [ ] Verify backend parity, schema migration, source expiry, conflicting feedback, and verification arriving after a new procedure version.

## 3. Applicability, budget, cache, and disclosure
<!-- specs: memory/procedural -->

- [ ] RED: Add wrong-platform, unknown/mismatched required tool version, whole-unit budget overflow, stale eligibility cache, and authority-preservation cases.
- [ ] GREEN: Extend existing applicability, selection, renderer, and cache owners with procedure/evidence/policy/environment identities and complete prerequisites/validation accounting.
- [ ] RED/GREEN: Deduplicate unchanged promoted-skill/procedure lineage through existing disclosure; invalidate equivalence after human edits and verify admitted-snapshot changes invalidate selection.
- [ ] Verify retired/hypothesis guidance eligibility and unchanged runtime permission decisions without a second injector.

## 4. Strict skills validation and recoverable promotion
<!-- specs: memory/procedural -->

- [ ] RED/GREEN: Add strict diagnostics in `omegon-skills` for malformed YAML/TOML, missing required fields, activation and provenance conflicts; retain tolerant inventory parsing.
- [ ] RED: Add missing/stale approval, evidence-precondition change, unsupported scope/assets, destination race, operation-ID misuse, and exact replay fixtures.
- [ ] GREEN: Compose immutable plans and operator-bound apply through existing guarded project mutation owners; publish only prompt-only artifacts at absent destinations without force.
- [ ] GREEN: Persist operation journals, durable publication identity, receipts, and lineage; implement forward repair without a filesystem/database atomicity claim.
- [ ] Verify crash injection before/after intent, publication, and receipt persistence, including reopen, matching bytes without ownership, changed/deleted targets, and intervening human edits.
- [ ] Verify artifact creation grants no path/executable/global/ambient trust and does not bypass normal activation/admission.

## 5. Shared host integration after interface freeze
<!-- specs: memory/procedural -->

- [ ] Register promotion and inspection through the command registry and shared semantic surfaces with availability, safety, and provenance metadata.
- [ ] Implement TUI, CLI remote/control-runtime, and ACP adapters against frozen DTOs; test real dispatch rather than assuming `/skills` metadata proves parity.
- [ ] Verify consistent plans, approvals, conflicts, diagnostics, recovery states, receipts, and unavailable results across adapters.
- [ ] Verify live composition uses existing reload, activation, and disclosure ownership while preserving admitted snapshots.

## 6. Application quality and landing evidence
<!-- specs: memory/procedural -->

- [ ] After interface freeze, develop fixtures in parallel with owner implementation: actual application tasks, repeated-failure opportunities, negative transfer, and held-out tool/environment versions.
- [ ] Extend accepted Wave 5 evaluation infrastructure with independent-application denominators and formation/reconciliation/selection/application/verification/promotion attribution.
- [ ] Freeze development-derived acceptance thresholds and obtain a fresh explicit live model/token/time/cost budget before held-out dispatch; retain unavailable/uncertain outcomes without invented thresholds.
- [ ] Record scenario-to-test mappings and deterministic red/green evidence; report repeated-error improvement only where opportunities and comparative evidence support it.
- [ ] Run narrow owner tests during implementation and required memory persistence/reopen and host routing tests; land multi-crate work with `just test-commit` and `just clippy-changed` under root guidance.
- [ ] Validate the named OpenSpec change and whitespace, then record implementation and quality evidence separately before requesting archival.
