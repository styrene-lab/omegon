# release-plz configuration for the omegon workspace.
#
# Defaults: don't publish workspace crates from this repository. The public
# extension SDK now lives in the standalone omegon-extension-rs repository and
# is released there.

[workspace]
publish = false
release = false
