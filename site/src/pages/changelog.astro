---
import Docs from "../layouts/Docs.astro";
import { readRepoDoc } from "../lib/repo-docs";

// Throws (failing the build) if CHANGELOG.md can't be read — a placeholder
// changelog in production is worse than a red CI run.
const raw = readRepoDoc("CHANGELOG.md");

const { default: markdownit } = await import("markdown-it");
const md = markdownit({ html: true, linkify: true });
const html = md.render(raw);
---

<Docs title="Changelog" description="Release history for omegon.">
  <Fragment set:html={html} />
</Docs>
