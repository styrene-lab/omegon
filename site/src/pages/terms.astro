---
import Docs from "../layouts/Docs.astro";
import { readRepoDoc } from "../lib/repo-docs";

// Throws (failing the build) if TERMS.md can't be read.
const raw = readRepoDoc("TERMS.md");

const { default: markdownit } = await import("markdown-it");
const md = markdownit({ html: true, linkify: true });
const html = md.render(raw);
---

<Docs title="Terms of Use" description="Terms of use, licensing, and legal information for Omegon by Black Meridian, LLC.">
  <Fragment set:html={html} />
</Docs>
